# For anyone who reaches for npm in this repo — Collie itself installs with Bun, and Bun reads the
# same cooldown from bunfig.toml (it does not understand this key). Keep the two the same.
min-release-age=7
